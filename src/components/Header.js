import React from 'react';
import Navbar from './Navbar';

const Header = () => {
    return (
        <div>
            <Navbar></Navbar>
            <hr />
        </div>
    );
};

export default Header;